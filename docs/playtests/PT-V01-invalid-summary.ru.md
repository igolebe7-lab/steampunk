# PT-V01: технически недействительная сессия

- Сборка: `ed184df`
- Исход: `aborted`
- Длительность: `05:29`
- Валидность: технически недействительна; в расчёт `4/5` не входит

Сессию проводил владелец проекта, уже знакомый с игрой. Во время проверки обнаружен технический дефект интерфейса: постоянная контекстная панель перекрывала полезную область, а макет `1280×720` выглядел сжатым. Поэтому поведение участника не используется для выводов о понятности или балансе.

Локальный журнал не содержал отклонённых команд; состояние готовности первого удара было достигнуто примерно к `05:07`. Эти данные сохранены только как техническая диагностика и не являются результатом слепого плейтеста.

Причина устранена переходом на полноразмерный адаптивный HUD. Поскольку изменение влияет на прохождение, валидная серия начинается заново на одном новом неизменном SHA с идентификаторов `PT-R01`…`PT-R05`. Сырой JSON и сгенерированный отчёт не коммитятся.
